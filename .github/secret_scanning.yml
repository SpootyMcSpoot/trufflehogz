# GitHub Secret Scanning Configuration
# Exclude test fixtures containing intentional fake secrets for TruffleHog testing

paths-ignore:
  - 'test/fixtures/**'
  - 'test/test-config.json'
  - '.env.example'
