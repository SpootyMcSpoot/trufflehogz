# Workflow Linting — validates all .github/workflows/*.yml on every PR
# Adapted from slac-templates/lint-workflows.yml
# Uses actionlint for syntax/type/expression checks and ghalint for
# security-policy enforcement (permissions, SHA pinning, timeout, etc.)

name: Lint Workflows

on:
  pull_request:
    paths:
      - '.github/workflows/**'
  push:
    branches: [main]
    paths:
      - '.github/workflows/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  actionlint:
    name: actionlint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          persist-credentials: false

      - name: Install actionlint
        run: |
          set -euo pipefail
          ACTIONLINT_VERSION="1.7.11"
          curl -sSLo actionlint.bash \
            "https://raw.githubusercontent.com/rhysd/actionlint/main/scripts/download-actionlint.bash"
          bash actionlint.bash "$ACTIONLINT_VERSION"
          echo "actionlint $ACTIONLINT_VERSION installed"

      - name: Run actionlint
        run: |
          set -euo pipefail
          ./actionlint -color -verbose

  ghalint:
    name: ghalint (security policies)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          persist-credentials: false

      - name: Install ghalint
        run: |
          set -euo pipefail
          GHALINT_VERSION="v1.5.5"
          curl -sSLo /usr/local/bin/ghalint \
            "https://github.com/suzuki-shunsuke/ghalint/releases/download/${GHALINT_VERSION}/ghalint_linux_amd64"
          chmod +x /usr/local/bin/ghalint
          echo "ghalint ${GHALINT_VERSION} installed"

      - name: Run ghalint
        env:
          GHALINT_LOG_COLOR: always
        run: |
          set -euo pipefail
          ghalint run
